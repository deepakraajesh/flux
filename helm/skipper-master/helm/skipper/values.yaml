# Default values for skipper.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: 012629307706.dkr.ecr.us-east-1.amazonaws.com/skipper
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "v1.10.0"

postInstallImage:
  repository: 012629307706.dkr.ecr.us-east-1.amazonaws.com/skipper-post-install
  pullPolicy: Always
  tag: "v1.10.0"

selectorLabels:
  app: skipper
  env: ssdev
  region: us-west-1
podAnnotations: {}

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""


podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 80
  annotations: {}

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths: []
  tls: []
  #  - secretName: chart-example-tls
  #    hosts:
  #      - chart-example.local

resources:
  # We usually recommend not to specify default resources and to leave this as a conscious
  # choice for the user. This also increases chances charts run on environments with little
  # resources, such as Minikube. If you do want to specify resources, uncomment the following
  # lines, adjust them as necessary, and remove the curly braces after 'resources:'.
  limits:
     cpu: 900m
     memory: 2048Mi
  requests:
     cpu: 600m
     memory: 2048Mi

AWS_ACCESS_KEY_ID: "QUtJQVFGNEdENkU1SVVRVlJTVVk="
AWS_SECRET_ACCESS_KEY: "M0dmdVJGc0ZGczJ5SHJSYnNxbmJQMGlWaDdnWXdzWVdiM1dVRGxYcQ=="

envVars:
  - name: SYSTEM_OPTS
    value: "-Dssobase.url=http://ssdevconsole-ui.unbxd.io/ -Dconsole.url=https://ssdevconsole-ui.unbxd.io/ -Dpim.url=http://pim-dev.unbxd.io/ -Dmongo=mongodb://mongos-headless-service.search:27017 -Dpim-searchapp.url=https://shopify-ssdev.unbxd.io/setup/  -Dfeed.url=http://wingman-feed.search/ -Dfeed.healthcheck=monitor -Dgimli.url=http://gimli.search/ -Dgimli.healthcheck=api/getHealthStatus.do -Drelevancy.url=http://relevance-workflow -Drelevancy.healthcheck=/v1.0/relevancy/live  -Dconsole-backend.url=https://ssdevconsole-nam.unbxd.io/ -Dsearch.url=http://brewer.search/ -Dsearch.healthcheck=monitor -Ddomain.name=skipper.argocd.qa1.us-west-1.infra  -Dasterix.url=http://asterix.search/ -Dasterix.healthcheck=monitor -DHagrid.url=http://hagrid-ambassador.search/ -DHagrid.healthcheck=hello -Dconsumer.url=http://toucan -Dregion=us -Daws-region=us-west-1 -Dmozart.url=http://mozart.search -Dpim.internal.url=http://pimdev.unbxd.io/paprika/ -Dpim.internal.healthcheck=/monitor   -DselfServe.s3Bucket=ss-unbxd-us-west-1 -DAWS_DEFAULT_REGION=us-west-1 -Dshopify.plugin.url=https://shopify-ssdev.unbxd.io/ -Dconsole.prod.url=https://console-nam.unbxd.io/ -Dtest=true -DautosuggestThreshold=0.0f"

mongo:
 connectionString: "mongodb://mongos-headless-service.search:27017"
nodeSelector: {}

tolerations: []

affinity: {}
